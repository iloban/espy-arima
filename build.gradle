subprojects {
    apply plugin: 'java'

    sourceCompatibility = 1.8
    version = '0.1-SNAPSHOT'

    repositories {
        jcenter()
    }

    dependencies {
        testCompile 'junit:junit:4.11'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}
